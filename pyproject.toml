[project]
name = "blog"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "baml-py>=0.226.2",
]

[dependency-groups]
dev = [
    "hemoglobin>=0.0.11",
    "textstat-cli>=1.1.3",
]
